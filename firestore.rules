rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // CV documents - only the owner can read/write their own data
    match /cvs/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
